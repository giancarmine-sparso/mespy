[project]
name = "mech-lab-tools"
version = "0.1.0"
description = "Toolbox Python per l'analisi dati del laboratorio di meccanica"
readme = { file = "README.md", content-type = "text/markdown" }
license = "MIT"
license-files = ["LICENSE"]
authors = [{ name = "Giancarmine-Sparso", email = "giancarminesparso@gmail.com" }]
requires-python = ">=3.12"
keywords = ["physics", "statistics", "data analysis", "laboratory", "mechanics"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.12",
    "Topic :: Scientific/Engineering :: Physics",
]
dependencies = [
    "numpy",
    "pandas",
    "matplotlib",
    "scipy",
]

[project.urls]
Homepage = "https://github.com/giancarmine-sparso/mech-lab-tools"
Repository = "https://github.com/giancarmine-sparso/mech-lab-tools"
Issues = "https://github.com/giancarmine-sparso/mech-lab-tools/issues"

[project.optional-dependencies]
dev = [
    "build",
    "pytest",
    "setuptools>=68",
    "twine",
    "wheel",
    "ipykernel",
]

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"
